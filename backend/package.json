{
  "name": "tradeflow-backend",
  "private": true,
  "workspaces": [
    "api-gateway",
    "auth-service",
    "lc-service"
  ],
  "scripts": {
    "dev:all": "npm run dev --workspaces --if-present"
  }
}
